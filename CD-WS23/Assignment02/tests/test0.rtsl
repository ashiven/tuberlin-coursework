/*
  Test 0, simple empty shader.
  Lexical and parsing error-free.


  In the GLSL grammar it would be similar to:

  translation_unit ->
  external_declaration ->
  declaration -> 
  init_declarator_list SEMICOLON -> 
  single_declaration SEMICOLON -> 
  fully_specified_type IDENTIFIER EQUAL initializer SEMICOLON ->
  type_specifier IDENTIFIER EQUAL initializer SEMICOLON ->
  type_specifier_nonarray IDENTIFIER EQUAL primary_expression SEMICOLON ->

  There is no CLASS in GLSL type_specifier_nonarray 
  and there is no RT_MATERIAL in primary_expression

  CLASS IDENTIFIER EQUAL RT_MATERIAL SEMICOLON
*/ 

class Test0 : rt_Material;



